# Backend Environment Variables for DocAgent Arena
# Copy this file to .env and fill in your actual values

# ============================================================================
# Database (Supabase PostgreSQL)
# ============================================================================
# Direct connection (for migrations, schema changes) - Port 5432
DIRECT_URL=postgresql://user:password@your-project.pooler.supabase.com:5432/postgres

# Pooled connection (for queries, transactions) - Port 6543
# IMPORTANT: Add ?pgbouncer=true for Supabase transaction pooling compatibility
# This disables prepared statements to prevent "prepared statement already exists" errors
DATABASE_URL=postgresql://user:password@your-project.pooler.supabase.com:6543/postgres?pgbouncer=true

# ============================================================================
# RAG Provider API Keys
# ============================================================================

# OpenAI (used by LlamaIndex for embeddings/LLM)
OPENAI_API_KEY=your_openai_key_here

# LlamaCloud (for LlamaParse PDF parsing)
LLAMAINDEX_API_KEY=your_llamaindex_cloud_key_here

# LandingAI (Agentic Document Extraction)
# Supports API key pooling: use comma-separated keys for automatic fallback
# Single key: VISION_AGENT_API_KEY=key1
# Multiple keys (pool): VISION_AGENT_API_KEY=key1,key2,key3
# If a key fails due to quota/credit issues, the system automatically tries the next one
VISION_AGENT_API_KEY=your_landingai_ade_api_key_here

# Reducto (Document Processing)
REDUCTO_API_KEY=your_reducto_api_key_here

# ============================================================================
# LLM API Keys (for scoring/evaluation)
# ============================================================================

# Anthropic Claude (for evaluation)
ANTHROPIC_API_KEY=your_anthropic_key_here

# ============================================================================
# Optional: Additional Provider API Keys
# ============================================================================

# Pinecone (if using as RAG provider)
# PINECONE_API_KEY=your_pinecone_key_here

# Weaviate (if using as RAG provider)
# WEAVIATE_API_KEY=your_weaviate_key_here

# ============================================================================
# Deployment Notes
# ============================================================================
# For Railway/production deployment:
# - Set these as environment variables in Railway dashboard
# - Do NOT commit .env file to git
# - Use DIRECT_URL for Prisma migrations
# - Use DATABASE_URL for application queries
